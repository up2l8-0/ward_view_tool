require "spec_helper"

describe WardViewTool do
  it "has a version number" do
    expect(WardViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
